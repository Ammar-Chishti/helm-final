apiVersion: v1
kind: Service
metadata:
  name: {{ include "ammar-eighth-service.fullname" . }}
  labels:
    {{- include "ammar-eighth-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ammar-eighth-service.selectorLabels" . | nindent 4 }}
