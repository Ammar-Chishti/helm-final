apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ammar-eighth-service.fullname" . }}-test-connection"
  labels:
    {{- include "ammar-eighth-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ammar-eighth-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
